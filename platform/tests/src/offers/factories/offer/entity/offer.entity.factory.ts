/**
 * Offer Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: offers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { OfferEntity } from "@offertrail/core/offers/models/index.js";
import { offersSchemas } from "@offertrail/core/offers";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "ofr";

export function createOffer(overrides: Partial<OfferEntity> = {}) {
  const raw = {
    id: overrides.id ?? `ofr_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    dataCategories: overrides.dataCategories ?? [],
    journeyId: overrides.journeyId ?? `jrn_${ulid().toLowerCase()}`,
    name: overrides.name ?? faker.person.fullName(),
    purpose: overrides.purpose ?? faker.lorem.word(),
    retentionIntent: overrides.retentionIntent ?? faker.lorem.word(),
    status: overrides.status ?? "draft",
    thirdPartySharing: overrides.thirdPartySharing ?? "excluded",
    valueType: overrides.valueType ?? "discount",
    version: overrides.version ?? faker.number.int(),
    ...overrides,
  };

  return offersSchemas.Offer.parse(raw);
}

export function offerBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createOffer(overrides));
}
