/**
 * UpdateOffer handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: offers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateOffer } from "@offertrail/api-server/domains/offers/handlers";
import type { OffersDDDDependencies } from "@offertrail/api-server/domains/offers";
import { createOffer as createOfferEntity } from "@offertrail/factories/offers";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@offertrail/tests/shared";
import { OFFERS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateOffer", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: OffersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<OffersDDDDependencies>(
      OFFERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createOfferEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { offerId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateOffer, "execute").mockResolvedValue(useCaseResult);

    await updateOffer(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateOffer.execute).toHaveBeenCalledWith(expect.objectContaining({ offerId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
