/**
 * Offers Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: offers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const createOffer_Body = z
  .object({
    journeyId: z.string().regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
    name: z.string().min(1).max(200),
    valueType: z.enum([
      'discount',
      'service_upgrade',
      'choosable_third_party_share',
    ]),
    valuePromise: z.string().optional(),
    purpose: z.string(),
    dataCategories: z.array(z.string()).min(1),
    retentionIntent: z.string(),
    thirdPartySharing: z.enum(['excluded', 'optional']).optional(),
    templateKey: z.string().optional(),
  })
  .passthrough();
const updateOffer_Body = z
  .object({
    name: z.string(),
    valuePromise: z.string(),
    purpose: z.string(),
    dataCategories: z.array(z.string()),
    retentionIntent: z.string(),
    thirdPartySharing: z.enum(['excluded', 'optional']),
  })
  .partial()
  .passthrough();
const recordOfferApproval_Body = z
  .object({
    role: z.enum(['business', 'privacy', 'technology']),
    decision: z.enum(['approved', 'rejected', 'change_requested']),
    rationale: z.string().optional(),
  })
  .passthrough();

export const schemas: any = {
  createOffer_Body,
  updateOffer_Body,
  recordOfferApproval_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v1/approvals',
    alias: 'listPendingApprovals',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'role',
        type: 'Query',
        schema: z.enum(['business', 'privacy', 'technology']).optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  id: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
                  journeyId: z.string().regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
                  name: z.string().min(1).max(200),
                  status: z.enum([
                    'draft',
                    'pending_approval',
                    'live',
                    'paused',
                    'retired',
                  ]),
                  valueType: z.enum([
                    'discount',
                    'service_upgrade',
                    'choosable_third_party_share',
                  ]),
                  valuePromise: z.string().optional(),
                  purpose: z.string(),
                  dataCategories: z.array(z.string()),
                  retentionIntent: z.string(),
                  thirdPartySharing: z.enum(['excluded', 'optional']),
                  templateKey: z.string().optional(),
                  version: z.number().int().gte(1),
                  collectionFrozen: z.boolean().optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }).optional(),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/offers',
    alias: 'listOffers',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'journeyId',
        type: 'Query',
        schema: z
          .string()
          .regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/)
          .optional(),
      },
      {
        name: 'status',
        type: 'Query',
        schema: z
          .enum(['draft', 'pending_approval', 'live', 'paused', 'retired'])
          .optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  id: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
                  journeyId: z.string().regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
                  name: z.string().min(1).max(200),
                  status: z.enum([
                    'draft',
                    'pending_approval',
                    'live',
                    'paused',
                    'retired',
                  ]),
                  valueType: z.enum([
                    'discount',
                    'service_upgrade',
                    'choosable_third_party_share',
                  ]),
                  valuePromise: z.string().optional(),
                  purpose: z.string(),
                  dataCategories: z.array(z.string()),
                  retentionIntent: z.string(),
                  thirdPartySharing: z.enum(['excluded', 'optional']),
                  templateKey: z.string().optional(),
                  version: z.number().int().gte(1),
                  collectionFrozen: z.boolean().optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }).optional(),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/offers',
    alias: 'createOffer',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: createOffer_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
            journeyId: z.string().regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
            name: z.string().min(1).max(200),
            status: z.enum([
              'draft',
              'pending_approval',
              'live',
              'paused',
              'retired',
            ]),
            valueType: z.enum([
              'discount',
              'service_upgrade',
              'choosable_third_party_share',
            ]),
            valuePromise: z.string().optional(),
            purpose: z.string(),
            dataCategories: z.array(z.string()),
            retentionIntent: z.string(),
            thirdPartySharing: z.enum(['excluded', 'optional']),
            templateKey: z.string().optional(),
            version: z.number().int().gte(1),
            collectionFrozen: z.boolean().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/offers/:offerId',
    alias: 'getOffer',
    requestFormat: 'json',
    parameters: [
      {
        name: 'offerId',
        type: 'Path',
        schema: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
            journeyId: z.string().regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
            name: z.string().min(1).max(200),
            status: z.enum([
              'draft',
              'pending_approval',
              'live',
              'paused',
              'retired',
            ]),
            valueType: z.enum([
              'discount',
              'service_upgrade',
              'choosable_third_party_share',
            ]),
            valuePromise: z.string().optional(),
            purpose: z.string(),
            dataCategories: z.array(z.string()),
            retentionIntent: z.string(),
            thirdPartySharing: z.enum(['excluded', 'optional']),
            templateKey: z.string().optional(),
            version: z.number().int().gte(1),
            collectionFrozen: z.boolean().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'patch',
    path: '/v1/offers/:offerId',
    alias: 'updateOffer',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: updateOffer_Body,
      },
      {
        name: 'offerId',
        type: 'Path',
        schema: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
            journeyId: z.string().regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
            name: z.string().min(1).max(200),
            status: z.enum([
              'draft',
              'pending_approval',
              'live',
              'paused',
              'retired',
            ]),
            valueType: z.enum([
              'discount',
              'service_upgrade',
              'choosable_third_party_share',
            ]),
            valuePromise: z.string().optional(),
            purpose: z.string(),
            dataCategories: z.array(z.string()),
            retentionIntent: z.string(),
            thirdPartySharing: z.enum(['excluded', 'optional']),
            templateKey: z.string().optional(),
            version: z.number().int().gte(1),
            collectionFrozen: z.boolean().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 409,
        description: `Idempotency key reuse with different body, or state conflict`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/offers/:offerId/approval',
    alias: 'recordOfferApproval',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: recordOfferApproval_Body,
      },
      {
        name: 'offerId',
        type: 'Path',
        schema: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^apr_[0-9A-HJKMNP-TV-Z]{26}$/),
            offerId: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
            role: z.enum(['business', 'privacy', 'technology']),
            decision: z.enum(['approved', 'rejected', 'change_requested']),
            rationale: z.string().optional(),
            decidedAt: z.string().datetime({ offset: true }),
            decidedBy: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/offers/:offerId/approvals',
    alias: 'listOfferApprovals',
    requestFormat: 'json',
    parameters: [
      {
        name: 'offerId',
        type: 'Path',
        schema: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  id: z.string().regex(/^apr_[0-9A-HJKMNP-TV-Z]{26}$/),
                  offerId: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
                  role: z.enum(['business', 'privacy', 'technology']),
                  decision: z.enum([
                    'approved',
                    'rejected',
                    'change_requested',
                  ]),
                  rationale: z.string().optional(),
                  decidedAt: z.string().datetime({ offset: true }),
                  decidedBy: z.string().optional(),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/offers/:offerId/submit-approval',
    alias: 'submitOfferForApproval',
    requestFormat: 'json',
    parameters: [
      {
        name: 'offerId',
        type: 'Path',
        schema: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            id: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
            journeyId: z.string().regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
            name: z.string().min(1).max(200),
            status: z.enum([
              'draft',
              'pending_approval',
              'live',
              'paused',
              'retired',
            ]),
            valueType: z.enum([
              'discount',
              'service_upgrade',
              'choosable_third_party_share',
            ]),
            valuePromise: z.string().optional(),
            purpose: z.string(),
            dataCategories: z.array(z.string()),
            retentionIntent: z.string(),
            thirdPartySharing: z.enum(['excluded', 'optional']),
            templateKey: z.string().optional(),
            version: z.number().int().gte(1),
            collectionFrozen: z.boolean().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Semantically invalid request (e.g. PACK_EMPTY)`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/offers/:offerId/versions',
    alias: 'listOfferVersions',
    requestFormat: 'json',
    parameters: [
      {
        name: 'offerId',
        type: 'Path',
        schema: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  id: z.string().regex(/^ovn_[0-9A-HJKMNP-TV-Z]{26}$/),
                  offerId: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
                  version: z.number().int().gte(1),
                  snapshot: z
                    .object({
                      id: z.string().regex(/^ofr_[0-9A-HJKMNP-TV-Z]{26}$/),
                      journeyId: z
                        .string()
                        .regex(/^jrn_[0-9A-HJKMNP-TV-Z]{26}$/),
                      name: z.string().min(1).max(200),
                      status: z.enum([
                        'draft',
                        'pending_approval',
                        'live',
                        'paused',
                        'retired',
                      ]),
                      valueType: z.enum([
                        'discount',
                        'service_upgrade',
                        'choosable_third_party_share',
                      ]),
                      valuePromise: z.string().optional(),
                      purpose: z.string(),
                      dataCategories: z.array(z.string()),
                      retentionIntent: z.string(),
                      thirdPartySharing: z.enum(['excluded', 'optional']),
                      templateKey: z.string().optional(),
                      version: z.number().int().gte(1),
                      collectionFrozen: z.boolean().optional(),
                      createdAt: z.string().datetime({ offset: true }),
                      updatedAt: z
                        .string()
                        .datetime({ offset: true })
                        .optional(),
                    })
                    .passthrough(),
                  createdAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
]);

export const api: any = new Zodios(
  'https://api.ddd-codegen-starter.local',
  endpoints
);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
