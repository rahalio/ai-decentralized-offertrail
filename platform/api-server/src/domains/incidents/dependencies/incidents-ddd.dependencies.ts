/**
 * Incidents DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incidents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Incidents Domain Module - Composition root
 *
 * DDD: Composition root for incidents domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  IncidentRepositoryAdapter,
} from "@offertrail/adapters/incidents";
import { getIdGeneratorService } from "@offertrail/adapters";
import type { AdapterDynamoDBClient } from "@offertrail/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteDeclareTrustIncident,
  ExecuteGetTrustIncident,
  ExecuteListTrustIncidents,
  ExecutePauseOffersForIncident,
  ExecuteResumeOffersForIncident,
} from "@offertrail/services/incidents/usecases";
// Ports (interfaces)
import type {
  IncidentRepository,
} from "@offertrail/services/incidents/ports";

export interface IncidentsDomainModule {
  repos: {
    incidents: IncidentRepository;
  };
  useCases: {
    incidents: {
      get: ExecuteGetTrustIncident;
      list: ExecuteListTrustIncidents;
    };
  };
}

export function buildIncidentsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): IncidentsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      incidents?: IncidentRepository;
    } = {};

    obj.incidents = new IncidentRepositoryAdapter(dynamoClient);
    return {
      incidents: obj.incidents!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    incidents: {
      get: new ExecuteGetTrustIncident(executionContext, idGenerator, repos.incidents),
      list: new ExecuteListTrustIncidents(executionContext, idGenerator, repos.incidents),
    },
  };
  return { repos, useCases: useCases as IncidentsDomainModule["useCases"] };
}
