/**
 * FulfilmentRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: fulfilments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * FulfilmentRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements FulfilmentRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  FulfilmentRepository,
} from "@offertrail/services/fulfilments";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { FulfilmentRepositoryDdb } from "./fulfilment-repository.ddb.js";

/**
 * FulfilmentRepositoryAdapter
 *
 * Implements FulfilmentRepository port for infrastructure layer.
 */
export class FulfilmentRepositoryAdapter implements FulfilmentRepository {
  private readonly ddb: FulfilmentRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new FulfilmentRepositoryDdb(this.dynamoClient);
  }

  async listFulfilments(input: Parameters<FulfilmentRepository['listFulfilments']>[0]): Promise<Awaited<ReturnType<FulfilmentRepository['listFulfilments']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listFulfilments(input);
  }

  async recordFulfilment(input: Parameters<FulfilmentRepository['recordFulfilment']>[0]): Promise<Awaited<ReturnType<FulfilmentRepository['recordFulfilment']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.recordFulfilment(input);
  }

  async getFulfilment(input: Parameters<FulfilmentRepository['getFulfilment']>[0]): Promise<Awaited<ReturnType<FulfilmentRepository['getFulfilment']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getFulfilment(input);
  }
}
